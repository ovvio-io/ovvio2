import React from 'react';
import { WorkspacesBar } from '../../workspaces-bar/index.tsx';

type WsSettingsBarProps = {};

export const WsSettingsBar: React.FC<WsSettingsBarProps> = ({}) => {
  return <WorkspacesBar></WorkspacesBar>;
};
